import { type } from "os";
import { FilterBooksProps } from "./interfaces";

function useFilterBooks() {

 const handleClear = () => {
  
}
  return {
    handleClear
  };
};
export default useFilterBooks;
